import { DefineComponent } from 'vue';
import Schedule from './index';

/**
 * Vue 3 wrapper. Use `<Schedule ... ref="scheduleRef" />` and access the
 * underlying instance via `scheduleRef.value?.current` (e.g. `current.addEvents(...)`).
 */
declare const ScheduleVue: DefineComponent<
    Schedule.Options,
    {},
    {
        el: HTMLElement | null;
        current: Schedule.Instance | null;
    }
>;

export default ScheduleVue;
